# JumpServer-Fabric API Bridge Configuration

# Server Configuration
API_PORT=3001

# mTLS Configuration
# IMPORTANT: mTLS is REQUIRED for production deployment!
# The server will automatically enable HTTPS with client certificate validation
# when these files exist. Run ./setup-mtls.sh to generate certificates.
#
# For testing only, the server will fall back to HTTP if certificates are missing.
TLS_KEY_PATH=./certs/server-key.pem
TLS_CERT_PATH=./certs/server-cert.pem
TLS_CA_PATH=./certs/ca-cert.pem

# Fabric Hot Chain Configuration
# ForensicLabMSP peer on hot chain
HOT_PEER_ENDPOINT=localhost:8051
HOT_PEER_HOST_ALIAS=peer0.forensiclab.hot.coc.com
HOT_CHANNEL_NAME=evidence-hot
HOT_CHAINCODE_NAME=coc

# Fabric Cold Chain Configuration
# ForensicLabMSP peer on cold chain
COLD_PEER_ENDPOINT=localhost:10051
COLD_PEER_HOST_ALIAS=peer0.forensiclab.cold.coc.com
COLD_CHANNEL_NAME=evidence-cold
COLD_CHAINCODE_NAME=coc

# Fabric Identity (MSP)
# IMPORTANT: The actual certificate filename is Admin@forensiclab.hot.coc.com-cert.pem (not cert.pem)
MSP_ID=ForensicLabMSP
FABRIC_CERT_PATH=../blockchain/hot-chain/crypto-config/peerOrganizations/forensiclab.hot.coc.com/users/Admin@forensiclab.hot.coc.com/msp/signcerts/Admin@forensiclab.hot.coc.com-cert.pem
FABRIC_KEY_PATH=../blockchain/hot-chain/crypto-config/peerOrganizations/forensiclab.hot.coc.com/users/Admin@forensiclab.hot.coc.com/msp/keystore/priv_sk
FABRIC_TLS_CERT=../blockchain/hot-chain/crypto-config/peerOrganizations/forensiclab.hot.coc.com/tlsca/tlsca.forensiclab.hot.coc.com-cert.pem

# IPFS Configuration (OPTIONAL - will use mock CIDs if not available)
# Leave this as-is. The server will automatically detect if IPFS is unavailable
# and generate mock CIDs instead. Real IPFS can be added later.
IPFS_URL=http://localhost:5001
